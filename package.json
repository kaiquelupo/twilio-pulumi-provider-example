{
  "name": "twilio-pulumi-provider-example",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "ci:deploy-resources": "export $(cat .$BRANCH_NAME.env | xargs) && pulumi up --stack $BRANCH_NAME --yes",
    "ci:preview-resources": "export $(cat .$BRANCH_NAME.env | xargs) && pulumi preview --stack $BRANCH_NAME",
    "deploy-resources": "export $(cat .dev.env | xargs) && pulumi up --stack $BRANCH_NAME",
    "preview-resources": "export $(cat .dev.env | xargs) && pulumi preview --stack $BRANCH_NAME",
    "watch-resources": "export $(cat .dev.env | xargs) && pulumi watch --stack $BRANCH_NAME"
  },
  "dependencies": {
    "twilio-pulumi-provider": "0.0.4"
  },
  "devDependencies": {
    "@types/node": "^14.6.4"
  }
}
