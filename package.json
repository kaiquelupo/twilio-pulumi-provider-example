{
  "name": "cicd-example",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "ci:deploy-resources": "export $(cat .$BRANCH_NAME.env | xargs) && pulumi up --stack $BRANCH_NAME --yes",
    "ci:preview-resources": "export $(cat .$BRANCH_NAME.env | xargs) && pulumi preview --stack $BRANCH_NAME"
  },
  "devDependencies": {
    "@pulumi/pulumi": "^2.0.0",
    "@types/lodash": "^4.14.150",
    "lodash": "^4.17.15",
    "path": "^0.12.7",
    "twilio": "^3.47.0",
    "twilio-run": "^2.8.0-beta.1"
  },
  "dependencies": {
    "@twilio-labs/serverless-api": "^4.0.1",
    "@types/dotenv": "^8.2.0",
    "@types/node": "^14.6.0",
    "bluebird": "^3.7.2",
    "dotenv": "^8.2.0"
  }
}
